{
  "name": "@project/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet — add them on example branches'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.0",
    "@project/db": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}